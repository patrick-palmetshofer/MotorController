# MotorController

Controls a Nanotec N5 motor controller via MODBUS TCP. The N5 controller uses an in MODBUS encapsulated CANopen interface. See libmodbus repository for encapsulated CANopen support.

This library was used to control motor controllers via LabView
